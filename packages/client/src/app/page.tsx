import GeneratorPrompt from './(generator)/GeneratorPrompt';
import Header from './(header)/Header';

const Home = () => {
  return (
    <>
      <Header />
      <GeneratorPrompt />
    </>
  );
};

export default Home;
